# Copy to .env and fill in locally. NEVER commit real secrets.
# Service-specific variables live in each service's own .env.example.

# --- Options market data (see services/gex-engine) ---
# MARKET_DATA_PROVIDER=          # e.g. intrinio | theta | databento | polygon
# MARKET_DATA_API_KEY=

# --- Platform ---
# NODE_ENV=development
